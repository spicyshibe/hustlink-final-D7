<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .job-card {
            transition: transform 0.3s;
            height: 100%;
        }
        .job-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        .status-badge {
            font-size: 0.8em;
        }
        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">HustLink</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if (user) { %>
                        <li class="nav-item">
                            <span class="nav-link">Welcome, <%= user.role === 'admin' ? 'Admin' : user.username %></span>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard">Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">Logout</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link active" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/jobs">All Jobs</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Register</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="jumbotron bg-light p-5 rounded">
            <h1 class="display-4">Welcome to HustLink</h1>
            <p class="lead">Your Integrated Career Portal - Find the perfect job opportunity that matches your skills and aspirations.</p>
            <hr class="my-4">
            <p>Browse through hundreds of job listings from various companies and industries.</p>
            <% if (!user) { %>
                <div class="mt-4">
                    <a class="btn btn-primary btn-lg" href="/register" role="button">Get Started</a>
                    <a class="btn btn-outline-primary btn-lg" href="/login" role="button">Login</a>
                </div>
            <% } %>
        </div>

        <% if (!user) { %>
            <div class="alert alert-info">
                <i class="fas fa-info-circle"></i> <a href="/login" class="alert-link">Login</a> or <a href="/register" class="alert-link">Register</a> to apply for jobs
            </div>
        <% } %>

        <h2 class="mb-4">Current Job Openings</h2>
        
        <% if (jobs && jobs.length > 0) { %>
            <div class="row">
                <% jobs.forEach(job => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card job-card h-100">
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title"><%= job.title %></h5>
                                <h6 class="card-subtitle mb-2 text-muted">
                                    <span class="badge bg-info"><%= job.category_name %></span>
                                </h6>
                                <p class="card-text flex-grow-1"><%= job.description.substring(0, 120) %>...</p>
                                <div class="mt-auto">
                                    <p class="mb-1"><strong>Deadline:</strong> <%= new Date(job.deadline).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></p>
                                    <p class="mb-3">
                                        <strong>Status:</strong> 
                                        <span class="badge <%= job.status === 'Open' ? 'bg-success' : 'bg-danger' %>">
                                            <%= job.status %>
                                        </span>
                                    </p>
                                    <div class="d-flex justify-content-between">
                                        <a href="/jobs/<%= job.id %>" class="btn btn-primary">View Details</a>
                                        <% if (user && user.role === 'user') { %>
                                            <form action="/apply/<%= job.id %>" method="POST" class="d-inline">
                                                <button type="submit" class="btn btn-success">Apply Now</button>
                                            </form>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="alert alert-warning">
                <i class="fas fa-exclamation-triangle"></i> No job openings available at the moment. Please check back later.
            </div>
        <% } %>
    </div>

    <%- include('partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>