<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= job.title %> - HustLink</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .job-header {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            padding: 30px;
            border-radius: 10px;
            margin-bottom: 30px;
        }
        .requirements-list {
            list-style-type: none;
            padding-left: 0;
        }
        .requirements-list li {
            padding: 5px 0;
            border-bottom: 1px solid #eee;
        }
        .requirements-list li:last-child {
            border-bottom: none;
        }
        .deadline-alert {
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.7; }
            100% { opacity: 1; }
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">HustLink</a>
            <div class="navbar-nav ms-auto">
                <% if (user) { %>
                    <a class="nav-link" href="/dashboard">Dashboard</a>
                    <a class="nav-link" href="/jobs">All Jobs</a>
                    <a class="nav-link" href="/logout">Logout</a>
                <% } else { %>
                    <a class="nav-link" href="/">Home</a>
                    <a class="nav-link" href="/jobs">Jobs</a>
                    <a class="nav-link" href="/login">Login</a>
                    <a class="nav-link" href="/register">Register</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <a href="/jobs" class="btn btn-outline-secondary mb-3">← Back to All Jobs</a>
        
        <div class="job-header">
            <div class="row">
                <div class="col-md-8">
                    <h1 class="display-6"><%= job.title %></h1>
                    <div class="mb-3">
                        <span class="badge bg-info p-2 fs-6"><%= job.category_name %></span>
                        <span class="badge <%= job.status === 'Open' ? 'bg-success' : 'bg-danger' %> p-2 fs-6 ms-2">
                            <%= job.status %>
                        </span>
                    </div>
                </div>
                <div class="col-md-4 text-end">
                    <div class="alert <%= new Date(job.deadline) < new Date() ? 'alert-danger' : 'alert-warning' %> deadline-alert">
                        <strong>Application Deadline:</strong><br>
                        <%= new Date(job.deadline).toLocaleDateString('en-US', { 
                            weekday: 'long', 
                            year: 'numeric', 
                            month: 'long', 
                            day: 'numeric' 
                        }) %>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="row">
            <div class="col-md-8">
                <div class="card mb-4">
                    <div class="card-header">
                        <h4>Job Description</h4>
                    </div>
                    <div class="card-body">
                        <p class="card-text"><%= job.description %></p>
                    </div>
                </div>
                
                <div class="card mb-4">
                    <div class="card-header">
                        <h4>Requirements</h4>
                    </div>
                    <div class="card-body">
                        <% 
                            const requirements = job.requirement.split('\n').filter(req => req.trim() !== '');
                        %>
                        <% if (requirements.length > 0) { %>
                            <ul class="requirements-list">
                                <% requirements.forEach(req => { %>
                                    <li>✓ <%= req %></li>
                                <% }); %>
                            </ul>
                        <% } else { %>
                            <p><%= job.requirement %></p>
                        <% } %>
                    </div>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="card mb-4">
                    <div class="card-header">
                        <h5>Job Details</h5>
                    </div>
                    <div class="card-body">
                        <div class="mb-3">
                            <strong>Category:</strong><br>
                            <span class="badge bg-info"><%= job.category_name %></span>
                        </div>
                        <div class="mb-3">
                            <strong>Status:</strong><br>
                            <span class="badge <%= job.status === 'Open' ? 'bg-success' : 'bg-danger' %>">
                                <%= job.status %>
                            </span>
                        </div>
                        <div class="mb-3">
                            <strong>Posted Date:</strong><br>
                            <%= new Date(job.created_at).toLocaleDateString() %>
                        </div>
                        <div class="mb-3">
                            <strong>Deadline:</strong><br>
                            <%= new Date(job.deadline).toLocaleDateString() %>
                        </div>
                    </div>
                </div>
                
                <div class="card">
                    <div class="card-header">
                        <h5>Apply for this Position</h5>
                    </div>
                    <div class="card-body">
                        <% if (job.status === 'Closed') { %>
                            <div class="alert alert-danger">
                                This position is no longer accepting applications.
                            </div>
                        <% } else if (user && user.role === 'admin') { %>
                            <div class="alert alert-info">
                                Administrators cannot apply for jobs.
                            </div>
                        <% } else if (user && user.role === 'user') { %>
                            <p>Ready to apply for this position?</p>
                            <form action="/apply/<%= job.id %>" method="POST">
                                <div class="d-grid gap-2">
                                    <button type="submit" class="btn btn-success btn-lg">
                                        Submit Application
                                    </button>
                                </div>
                            </form>
                            <p class="text-muted mt-2" style="font-size: 0.9em;">
                                By applying, you agree to share your profile information with the employer.
                            </p>
                        <% } else { %>
                            <div class="alert alert-warning">
                                <h6>Login Required</h6>
                                <p>You need to be logged in as a job seeker to apply for this position.</p>
                                <div class="d-grid gap-2">
                                    <a href="/login" class="btn btn-primary">Login</a>
                                    <a href="/register" class="btn btn-outline-primary">Register</a>
                                </div>
                            </div>
                        <% } %>
                    </div>
                </div>
                
                <% if (user && user.role === 'admin') { %>
                    <div class="card mt-4">
                        <div class="card-header bg-dark text-white">
                            <h6>Admin Actions</h6>
                        </div>
                        <div class="card-body">
                            <div class="d-grid gap-2">
                                <a href="/jobs/<%= job.id %>/edit" class="btn btn-warning">Edit Job</a>
                                <form action="/jobs/<%= job.id %>/delete" method="POST">
                                    <button type="submit" class="btn btn-danger w-100" onclick="return confirm('Are you sure you want to delete this job?')">
                                        Delete Job
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <%- include('../partials/footer.ejs') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>